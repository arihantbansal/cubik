export interface NFTProfile {
  name: string;
  token: string;
  collection: string;
  owner: string;
}

export interface NftResponseCarousel {
  id: string;
  name: string;
  image: string;
  metadataName: string;
  tokenMint: string;
}
