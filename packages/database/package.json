{
  "name": "@cubik/database",
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push --skip-generate"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "dotenv": "^16.3.1",
    "dotenv-cli": "latest"
  },
  "devDependencies": {
    "prisma": "^5.0.0",
    "typescript": "^5.1.3"
  }
}