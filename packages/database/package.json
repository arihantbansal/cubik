{
  "name": "@cubik/database",
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "db:generate": "prisma generate && prisma format",
    "db:push": "prisma db push --skip-generate"
  },
  "dependencies": {
    "@prisma/client": "latest",
    "dotenv-cli": "latest"
  },
  "devDependencies": {
    "prisma": "latest"
  }
}